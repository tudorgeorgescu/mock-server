// Generated by CoffeeScript 1.3.3

/*
@license
encoders.js: Include for core encoders

(c) 2012 Panther Development
MIT LICENCE
*/


(function() {
  var common, encoders, fs, path;

  encoders = exports;

  fs = require("fs");

  path = require("path");

  common = require("./common");

  fs.readdirSync(path.join(__dirname, "encoders")).forEach(function(file) {
    var e, name;
    if (file === "encoders.coffee") {
      return;
    }
    e = file.replace(/.(js|coffee)/, "");
    name = common.titleCase(e);
    return encoders.__defineGetter__(name, function() {
      return require("./encoders/" + e);
    });
  });

}).call(this);
